<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <AssemblyName>OrderHub.NotificationService.Application</AssemblyName>
    <RootNamespace>OrderHub.NotificationService.Application</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Application yalnız Domain'e bağlıdır (Clean Arch). Infrastructure'a ASLA. -->
    <ProjectReference Include="..\NotificationService.Domain\OrderHub.NotificationService.Domain.csproj" />
  </ItemGroup>

  <!-- IServiceCollection (AddApplication extension method) için abstraction-only paket.
       MediatR/FluentValidation/Mapster YOK (5f-1 iskeleti — 5f-2'de gerektiğinde eklenir). -->
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
  </ItemGroup>

</Project>
