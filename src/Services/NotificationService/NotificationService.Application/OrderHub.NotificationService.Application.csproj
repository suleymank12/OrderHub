<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <AssemblyName>OrderHub.NotificationService.Application</AssemblyName>
    <RootNamespace>OrderHub.NotificationService.Application</RootNamespace>
  </PropertyGroup>

  <!-- Handler/job'lar internal (§9); Infrastructure Hangfire adapter + unit testler erişir. -->
  <ItemGroup>
    <!-- ICartAbandonmentScheduler.ScheduleReminder → BackgroundJobClient.Schedule<CartAbandonmentReminderJob> (DIP).
         CartAbandonmentReminderJob internal; Infrastructure Hangfire adapter onu tip olarak schedule eder. -->
    <InternalsVisibleTo Include="OrderHub.NotificationService.Infrastructure" />
    <InternalsVisibleTo Include="OrderHub.NotificationService.UnitTests" />
  </ItemGroup>

  <ItemGroup>
    <!-- Application yalnız Domain'e bağlıdır (Clean Arch). Infrastructure'a ASLA. -->
    <ProjectReference Include="..\NotificationService.Domain\OrderHub.NotificationService.Domain.csproj" />
  </ItemGroup>

  <!-- IServiceCollection (AddApplication), ILogger (CartAbandonmentReminderJob), IOptions abstraction'ları. -->
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
    <!-- CartAbandonmentReminderJob inline [LoggerMessage] kayıtları için. -->
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
  </ItemGroup>

</Project>
