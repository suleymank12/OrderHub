<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <AssemblyName>OrderHub.InventoryService.Api</AssemblyName>
    <RootNamespace>OrderHub.InventoryService.Api</RootNamespace>
  </PropertyGroup>

  <!-- internal tipler (ApiServicesExtensions vb.) yalnızca integration test assembly'sine açılır (§9). -->
  <ItemGroup>
    <InternalsVisibleTo Include="OrderHub.InventoryService.IntegrationTests" />
  </ItemGroup>

  <ItemGroup>
    <!-- 5c: Application (handler'lar + ExpireStockReservationsJob) + Infrastructure (DbContext + consumer'lar + outbox). -->
    <ProjectReference Include="..\InventoryService.Application\OrderHub.InventoryService.Application.csproj" />
    <ProjectReference Include="..\InventoryService.Infrastructure\OrderHub.InventoryService.Infrastructure.csproj" />
    <ProjectReference Include="..\..\..\BuildingBlocks\OrderHub.Observability\OrderHub.Observability.csproj" />
  </ItemGroup>

  <!-- JWT/Swashbuckle YOK: HTTP endpoint yok (command-driven, saga RabbitMQ ile). -->
  <ItemGroup>
    <PackageReference Include="Serilog.AspNetCore" />
    <PackageReference Include="Serilog.Sinks.Seq" />
    <PackageReference Include="Serilog.Sinks.File" />
    <PackageReference Include="AspNetCore.HealthChecks.SqlServer" />
    <PackageReference Include="AspNetCore.HealthChecks.UI.Client" />
    <!-- Hangfire hosting (server + SQL storage) Api'de: 15dk reservation expiry job'unu çalıştırır (ADR-0007 Karar 2).
         Dashboard YOK (YAGNI — JWT yok, UI gereksiz). Recurring kayıt Infrastructure'da (RecurringJobRegistrar). -->
    <PackageReference Include="Hangfire.AspNetCore" />
    <PackageReference Include="Hangfire.SqlServer" />
  </ItemGroup>

</Project>
