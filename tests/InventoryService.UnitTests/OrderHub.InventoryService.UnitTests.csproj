<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <AssemblyName>OrderHub.InventoryService.UnitTests</AssemblyName>
    <RootNamespace>OrderHub.InventoryService.UnitTests</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- 5b: yalnız Domain (aggregate davranış + invariant). Application 5c'de (handler testleri). -->
    <ProjectReference Include="..\..\src\Services\InventoryService\InventoryService.Domain\OrderHub.InventoryService.Domain.csproj" />
  </ItemGroup>

  <!-- IsTestProject / Test.Sdk / xunit / runner / coverlet + Using Xunit/FluentAssertions (global using)
       → tests/Directory.Build.props (ortak). FluentAssertions PAKETİ proje-bazlı eklenir. Domain testleri
       mock gerektirmez (saf aggregate) → Moq/AutoFixture yok. -->
  <ItemGroup>
    <PackageReference Include="FluentAssertions" />
  </ItemGroup>

</Project>
